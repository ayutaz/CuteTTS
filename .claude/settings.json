{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "c=$(jq -r '.tool_input.command // \"\"'); case \"$c\" in *'git commit'*) c=\"${c%%git commit*}\";; esac; if printf '%s' \"$c\" | grep -qE '(^|[;&|[:space:]])(ssh|scp|rsync|vastai)([[:space:]]|$)'; then exit 0; fi; if printf '%s' \"$c\" | grep -qE '(python[^;&|]*(reproduce_baseline|evaluate_japanese_vae|cache_audio_latents|build_voice_clusters|benchmark_training_memory|train_continual|evaluate_japanese_cer|diagnose_flow_loss|check_reference_following|infer)[.]py|--device[ =]+(cuda|auto)|(^|[;&|[:space:]])cutetts(-demo)?([[:space:]]|$)|pytest[^;&|]*-m[^;&|]*gpu)'; then jq -nc '{systemMessage:\"[GPU] ローカルGPU(4070 Ti SUPER 16GB)を使う可能性があります。事前確認を取りましたか？ 未確認なら中断してください（D-023）。\",hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"ask\",permissionDecisionReason:\"ローカルGPUを使う可能性があります。実行前にユーザーへ確認し、所要時間とVRAMの見積もりを伝えてください。GPUジョブは並列起動せず1本ずつ。重い処理はvast.aiへ回す（D-023/D-024）。ssh/scp/rsync/vastai の遠隔実行と、git commit のメッセージ本文は除外済み。\"}}'; fi",
            "timeout": 10,
            "statusMessage": "ローカルGPU使用をチェック中"
          }
        ]
      }
    ]
  }
}
